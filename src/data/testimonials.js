/**
 * Client testimonials / social proof.
 *
 * Add real quotes here — `quote` and `name` MUST come from the actual client,
 * never invented. While this array is empty the testimonials section renders
 * nothing, so nothing fake ever ships. @marketer polishes the copy; @user
 * approves before anything goes live.
 */
export const testimonials = [
  // {
  //   quote: '…',
  //   name: 'Client Name',
  //   role: 'Owner — Business', // optional
  //   business: 'Dips PT',      // optional
  //   rating: 5,                // optional, 1–5 — only render if genuinely earned
  //   result: '+38% bookings',  // optional short outcome
  // },
]
